import path from 'path';

export const BLOG_DIR = path.join(process.cwd(), 'public', 'content', 'blog');
export const GALLERY_DIR = path.join(
  process.cwd(),
  'public',
  'content',
  'gallery'
);
